# Computer Plugin

`@computer` is the Codex plugin wrapper for the Windows Computer Use Codex++ tweak.

The plugin intentionally does not declare a second MCP server. Codex++ manages the `windows-computer-use` MCP server from the `co.bennett.windows-computer-use` tweak, which keeps the product Windows-local and avoids duplicate tool registrations.
